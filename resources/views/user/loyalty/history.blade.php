@extends('layouts.user') @section('title','Lịch sử điểm - MovieMate')
@section('content')<main class="user-page-shell px-4 py-10"><div class="mx-auto max-w-4xl"><div class="cinema-card rounded-3xl p-6"><p class="app-muted">Thành viên MovieMate</p><p class="text-4xl font-extrabold text-ai-start">{{ number_format($account->points_balance,0,',','.') }}</p><p class="text-sm app-muted">điểm khả dụng · Tổng điểm đã tích: {{ number_format($account->lifetime_earned,0,',','.') }}</p></div><h1 class="mt-8 text-2xl font-extrabold app-text">Lịch sử điểm</h1><div class="mt-4 space-y-3">@forelse($transactions as $transaction)<div class="cinema-card flex justify-between gap-4 rounded-2xl p-4"><span><strong class="app-text">{{ $transaction->customer_description }}</strong><small class="block app-muted">{{ $transaction->created_at->format('d/m/Y H:i') }} · Số dư {{ number_format($transaction->balance_after,0,',','.') }} điểm</small></span><strong class="{{ $transaction->points_delta >= 0 ? 'text-success':'text-error' }}">{{ $transaction->points_delta >= 0 ? '+' : '' }}{{ number_format($transaction->points_delta,0,',','.') }}</strong></div>@empty<p class="app-muted">Chưa có giao dịch điểm.</p>@endforelse</div>{{ $transactions->links() }}</div></main>@endsection
